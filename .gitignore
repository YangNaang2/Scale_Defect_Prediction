# ============================================================
# 데이터 파일 — 대용량/생성 데이터는 기본적으로 전부 추적하지 않음.
# 다음 프로젝트에서도 재사용할 걸 감안해 확장자 자체를 넓게 막았다.
# 이 리포지토리에 한해 용량이 작은 원본 데이터셋 하나만 아래에서 예외 처리한다.
# ============================================================
*.csv
*.tsv
*.parquet
*.feather
*.xlsx
!data/raw/SCALE불량.csv

# ============================================================
# 모델 가중치 / 학습 아티팩트 — 용량이 크고 코드로 재생성 가능하므로 추적하지 않음
# ============================================================
*.joblib
*.pkl
*.pickle
*.pt
*.pth
*.onnx
*.h5
*.hdf5
*.ckpt
backend/artifacts/

# ============================================================
# DB / 로그 / 실행 중 생성되는 산출물
# ============================================================
*.db
*.sqlite
*.sqlite3
*.log
backend/batch_predict_result.csv
backend/.claude/skills/*/screenshots/

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.venv/
venv/
env/
ENV/

# ============================================================
# Node (프런트엔드 유닛테스트 / Claude Code 스킬 드라이버용 Playwright)
# ============================================================
node_modules/
npm-debug.log*

# ============================================================
# 환경변수 / 시크릿 — API 키, 토큰, 자격증명은 절대 커밋하지 않는다
# ============================================================
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.*

# ============================================================
# OS / 에디터
# ============================================================
.DS_Store
Thumbs.db
.vscode/
.idea/
